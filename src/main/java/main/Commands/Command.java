package main.Commands;

/**
 * * Interface defining the standard structure for all commands
 * Implements the Command design pattern
 * */
public interface Command {

    /**
     * * Executes the specific logic associated with the command
     * */
    void execute();
}
